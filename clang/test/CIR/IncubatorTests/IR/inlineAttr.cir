// RUN: cir-opt %s --verify-roundtrip | FileCheck %s -check-prefix=CIR
// RUN: cir-opt %s -cir-to-llvm -o - | FileCheck %s -check-prefix=MLIR

module {
  cir.func no_inline @l0() {
    cir.return
  }
}

// CIR: cir.func no_inline @l0()

// MLIR: llvm.func @l0() attributes {calling_conv = 1 : i32, cir.extra_attrs = #cir<extra({})>, global_visibility = #cir<visibility default>, no_inline
